// This file will contain common enums used across the application.
// Example:

enum ViewState {
  idle,
  loading,
  success,
  error,
}

// Add more enums as your application grows, for example:
// enum UserRole { admin, editor, viewer }
// enum SortOrder { ascending, descending }
