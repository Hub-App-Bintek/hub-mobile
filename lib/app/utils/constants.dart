// This file will hold application-wide constants.
// e.g., API keys (loaded from env, not hardcoded), default padding, animation durations.

// Example: (Remember to load sensitive keys from a secure source, not directly here)
// const String kApiKey = String.fromEnvironment('API_KEY', defaultValue: 'YOUR_FALLBACK_API_KEY');

const double kDefaultPadding = 16.0;
const Duration kDefaultAnimationDuration = Duration(milliseconds: 300);
